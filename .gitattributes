# Normalise line endings. Development happens on Windows but the application is
# deployed to Linux hosting, and a shell script checked out with CRLF fails on
# its own shebang line with a message that does not mention line endings.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
